<h1>Students</h1>

<div id="mentors">
  <% @students.each do |student| %>
    <%= render student %>
    <p>
      <%= link_to "Show this mentor", student %>
    </p>
    <p>
      <%= link_to "Edit this mentor", edit_student_path(student) %>
  <% end %>
</div>
